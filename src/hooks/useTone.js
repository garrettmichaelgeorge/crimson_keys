import { useEffect, useState } from "react";
import {
  synthConfig,
  distortionConfig,
  reverbConfig,
  feedbackDelayConfig,
} from "../data";
import * as Tone from "tone";

export default function useTone() {}
