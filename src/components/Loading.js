import React from 'react'

function Loading () {
  // render this component conditionally when components are loading,
  // i.e. in useEffect(func, [])

  return (
    <p>Loading...</p>
  )
}

export default Loading
